# aadpaguio.github.io

Personal site: short intro, selected projects, and technical writing. Built with [Quarto](https://quarto.org), served from `docs/` on GitHub Pages.

```bash
quarto preview    # local preview
quarto render     # write to docs/
```

After rendering, GitHub Pages should serve the `docs/` folder on `main`.
